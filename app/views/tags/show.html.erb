<div class="title">
  <h1> <%= @tag.name %> </h1>
</div>

<% if @tag.posts.empty? %>
  <h2>Статей нет</h2>
  <%else%>
  <%= render @tag.posts.includes(:tags, :category).order(:created_at).reverse_order %>
<% end %>
